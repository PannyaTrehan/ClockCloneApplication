import Header from "../components/Header";

export default function Timer() {
  return (
    <>
      <Header />
      <h2>Timer Page</h2>
    </>
  );
}

// use state for setting the timer
// a bool val to know if the timer is running or not
// reset timer function 
// formatted in min, second, millisecond --> all formatted in 2 floating point
// 
